{
  "name": "many-moons",
  "version": "1.0.0",
  "private": true,
  "description": "A quiet companion for the moon overhead — tonight's phase, and the stories the world has told about it.",
  "scripts": {
    "build:web": "node scripts/copy-web.mjs",
    "sync": "npm run build:web && cap sync",
    "open:android": "cap open android",
    "run:android": "npm run build:web && cap run android"
  },
  "dependencies": {
    "@capacitor/android": "^8.4.1",
    "@capacitor/app": "^8.1.0",
    "@capacitor/cli": "^8.4.1",
    "@capacitor/core": "^8.4.1",
    "@capacitor/filesystem": "^8.1.2",
    "@capacitor/local-notifications": "^8.2.0",
    "@capacitor/share": "^8.0.1",
    "@capacitor/status-bar": "^8.0.2"
  }
}
